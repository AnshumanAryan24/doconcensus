services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  votemanager:
    image: ./votemanager-image
    ports:
      - "8081:8080"
    environment:
      - REDIS_HOSTNAME=redis
      - REDIS_PORT=6379
    depends_on:
      - redis