services:
  redis:
    image: redis:alpine

  votemanager:
    image: votemanager-image:v1
    ports:
      - "8081:8080"
    environment:
      - REDIS_HOSTNAME=redis
      - REDIS_PORT=6379
    depends_on:
      - redis